{
  "name": "@whiterabbit/client",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -d index.ts --outDir dist/",
    "prepublish": "npm run prebuild && npm run build"
  },
  "devDependencies": {
    "typescript": "^3.8.3"
  }
}
